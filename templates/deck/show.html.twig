{% extends 'auth_base.html.twig' %}

{% block title %}{{parent()}}-Visualiser Deck{% endblock %}
{% block body %}
<br>
<br>
<center>
    <div class="container">
        <div class="row">
                <div class='col'>
                    <div class="container">
                        <p class="deck-view">Deck name :</p>
                        <p>
                            <i>{{ deck.name }}</i>
                        </p>
                        <p class="deck-view">Cards Number :</p>
                        <p>
                            <i>You have <b>{{ deck.maxsize }}</b> cards</i>
                        </p>
                        <p class="deck-view">Deck info :</p>
                        <p>
                            <i>{{ deck.description }}</i>
                        </p>
                    </div>
                </div>
        </div>
        <div class='align-items-center'>
            <a class="btn btn-light" href="{{path('deck_index')}}" role="button">⬅️ Back</a>
            <a class="btn btn-light" data-bs-toggle="modal" data-bs-target="#exampleModal" role="button">🗑️</a>
        </div>
    </div>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Deleted Deck</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        You will delete your deck
        are you sure ?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-bs-dismiss="modal">No</button>
        <a class="btn btn-success" href="{{path('deck_delete', {'name':deck.name}) }}?token={{ csrf_token('deck_deleted') }}" role="button">Yes</a>
      </div>
    </div>
  </div>
</div>
    <br>
    {#<nav>
        <ul class="pagination justify-content-center">
            <li class="page-item disabled">
                <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
            </li>
            <li class="page-item"><a class="page-link" href="#">1</a></li>
            <li class="page-item active" aria-current="page">
                <a class="page-link" href="#">2</a>
            </li>
            <li class="page-item"><a class="page-link" href="#">3</a></li>
            <li class="page-item">
                <a class="page-link" href="#">Next</a>
            </li>
        </ul>
    </nav>#}
</center>
{% endblock %}